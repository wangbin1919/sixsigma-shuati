/* ============================================================
 * 六西格玛刷题题库 · 微信群成员免费使用配置
 * ------------------------------------------------------------
 * 管理员只需修改本文件并重新部署 GitPages，即可：
 *   - 开新群：生成口令 -> 把哈希与群名加进 codes -> 重新部署
 *   - 撤回口令：把对应行从 codes 删除 -> 重新部署（即刻全局生效）
 *   - 调整免费体验额度：改 trialPerDay
 *
 * 安全说明：口令【明文】只发给群成员，本文件只存 SHA-256 哈希，
 * 防止源码泄露后口令被直接抄走；口令空间足够大，穷举不可行。
 *
 * 口令生成：python tools/make_code.py
 * ============================================================ */
window.SS_PASSPORT = {
  /* 全量免费开放：true 表示任何访客无需进群、无需口令即可刷全部题库（与一建题库一致）。
     开启后下方 trialPerDay/codes 仅保留兼容，实际不再拦截作答。 */
  freeForAll: true,

  /* 未解锁用户每日免费体验题数（作答后才扣减，跳过不扣） */
  trialPerDay: 15,

  /* 群口令有效期（天），到期后自动回到体验模式，需重新输入口令 */
  unlockDays: 30,

  /* 各群口令哈希：字段 = [口令SHA-256哈希小写, 群名, 有效天数(0=永久)]
     有效天数设为 0 表示永久有效；如需限时(如 30 天)则写数字。
     tools/make_code.py 生成时会直接打印这一行，粘贴到这里即可。 */
  codes: [
    /* 初始示例口令（注意只在测试阶段开放，正式运营请轮换）：
       口令明文：SS-DZHR-G44Z-4V4T-7XXP
       轮换方法：运行 python tools/make_code.py 生成新口令替换下面一行 */
    ["306bf743291f7e0fa0b1f5cd1652649c4ff384c82cf7a587e787121aa7a78c74", "六西格玛黑带备考群", 30],
  ]
};